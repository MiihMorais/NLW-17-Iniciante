//arrays, obejtos

let meta = {
    value: 'ler um livro por mês',
    checked: false,
    isChecked: (info) => {
        console.log(info)
    }
        
}

meta .value = "não é mais ler um"
meta.isChecked(meta.value)

//function //arrow function
const criarMeta = () => {}

//function criarMeta() {} named function